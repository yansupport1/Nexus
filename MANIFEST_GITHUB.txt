NEXUS-X v3.2.1 — GITHUB UPLOAD MANIFEST

Upload the contents of this package to the repository root yansupport1/Nexus.

Core:
- index.html — full APK/WebView source; legacy tools/features restored
- crypto.html — dark educational market prediction module
- market-service.js — public data service input; no trading writes
- assets/ — card, box, start, background, audio, font, logo files

Firebase / admin:
- firebase-app_config-v3.2.1.json — import only at the app_config node
- firebase-full-v3.2.1.json — full database example with demo data
- firebase-v3.2.1-seed.json — complete schema reference/seed; do not Replace at database root
- file.json — gateway metadata

Get Key:
- getkey/auto-create-key.html — writes global valid_keys/{KEY}; active key can be copied; 3s finalization animation

Global key rule:
- valid_keys/{KEY} and redeem_codes/{CODE} stay at root, outside app_config/versions.
- Version nodes use Firebase-safe names v3_1, v3_2, v3_2_1.
- If the current version node is missing/disabled, APK shows an update button and opens update_url/channel.
